# Telegram session — full account access, NEVER commit
session*.json
session*.txt

# Local config / secrets
*.local
.env
.env.*

# Personal config variants (config.yml stays tracked as the safe default;
# config.example.yml stays tracked as the documented reference).
config.*.yml
!config.example.yml

# Claude Code personal (non-shared) settings; not for the public template
.claude/settings.local.json

# Build output
bin/
/svodka
/login

# Go housekeeping
vendor/
*.test
*.out
coverage.*

# IDE / OS
.idea/
.vscode/
.DS_Store
