# Weekly dependency updates for Go modules and GitHub Actions, so the template
# does not drift out of date.
version: 2
updates:
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
